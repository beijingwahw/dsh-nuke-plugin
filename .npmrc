# dsh-tools 声明 9 个 peerDependencies（dsh-agent / dsh-llm / dsh-scope / dsh-session /
# dsh-code-runtime / dsh-invariants / dsh-system-prompt / dsh-user-approval / cordis）。
# 它们是 harness 运行时单例：类身份与 ctx 注册表必须与宿主共享，本仓库永远不安装
# （见 tsdown.config.ts 的 external 清单 —— 裸导入由 harness 根解析）。
# npm ≥7 默认自动补齐 peer，会试图把 8 个 @deepseek-ai 运行时包装进 devDependencies，
# 并触发 arborist PlaceDep 的上游崩溃（Cannot read properties of null (reading 'children')）。
# legacy-peer-deps = 回到 npm 6 语义：peer 只声明、不安装 —— 与本仓库 lock 的生成语义一致。
legacy-peer-deps=true
